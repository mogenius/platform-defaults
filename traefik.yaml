apiVersion: mogenius.com/v1alpha1
kind: PlatformDefault
spec:
  # renovate: datasource=helm registryUrl=https://helm.traefik.io/traefik chart=traefik
  version: 41.6.0
  valuesObject:
    deployment:
      kind: DaemonSet
    # Renamed in chart 41.x: this was logs.access.enabled until 39.x, and the
    # chart's schema rejects the old key outright rather than ignoring it.
    accessLog:
      enabled: true
    additionalArguments:
      - "--metrics.prometheus.addEntryPointsLabels=true"
      - "--metrics.prometheus.addServicesLabels=true"
      - "--metrics.prometheus.addRoutersLabels=true"
      - "--entrypoints.web.http.encodedcharacters.allowencodedslash=true"
      - "--entrypoints.websecure.http.encodedcharacters.allowencodedslash=true"
      - "--ping=true"
